## 第一段階
#### ハッシュテーブルの解法
こっちの方が好き。点検表を片手に記録して、都度ダブりの有無を確認するイメージ。
```python
class Solution(object):
    def hasCycle(self, head):
        """
        :type head: ListNode
        :rtype: bool
        """
        seen = set()
        
        while head is not None:
            if head in seen:
                return True
            
            seen.add(head)
            head = head.next
        
        return False
```

#### ウサギとカメの解法
```python
class Solution(object):
    def hasCycle(self, head):
        """
        :type head: ListNode
        :rtype: bool
        """
        # 遅い方が先に来て、早い方が後の方がいいと思ってこの並び。
        slow = head
        fast = head

        # 曖昧な記憶でやってみた。テストケース通ってしまった。
        while fast.next is not None and fast.next.next is not None:
            slow = slow.next
            fast = fast.next.next

            if slow == fast:
                return True
        
        return False
```
---
## 第二段階
#### ハッシュテーブルの解法
```python
class Solution(object):
    def hasCycle(self, head):
        """
        :type head: ListNode
        :rtype: bool
        """
        # 「こういうの」を全く考慮してなかった＝境界値
        if head is None:
            return False
    
        seen = set()
        # headは連結リストの先頭を指すものだから動くのは変だ、とコメントを見て納得。
        node = head

        while node is not None:
            if node in seen:
                return True
            
            seen.add(node)
            node = node.next
        
        return False
```
#### ウサギとカメの解法
```python
class Solution(object):
    def hasCycle(self, head):
        """
        :type head: ListNode
        :rtype: bool
        """
        # 「こういうの」を全く考慮してなかった＝境界値
        if head is None:
            return False

        # 同じ中身だし、一行で書く方がしっくり来る。
        slow, fast = head, head

        #これも多分 境界値。苦手。
        while fast is not None and fast.next is not None:
            slow = slow.next
            fast = fast.next.next

            if slow == fast:
                return True
        
        return False
```
- 以下の条件文について具体例を書いて理解はしたが腑に落ちず...また繰り返す気がするけどいいや
```python
while fast is not None and fast.next is not None:
```
<img width="2048" height="1146" alt="image" src="https://github.com/user-attachments/assets/ca865cdd-cb38-4ed2-abcb-626303fee5d8" />

- 計算量を初めて確認した
  - これを参考にした（https://github.com/Yuto729/leetcode/pull/16#discussion_r2602118324）
  - 最大の入力サイズは1万個。
  - ノードの中の値がマイナス10万からプラス10万まであり得る。今回は触らないから、何に気を付けるのかさっぱりわからない。
  - Pythonで実行しているので、最低 0.001秒で実行できる！

- Pythonのスタイルガイドラインを初めて開いた
    - PEP8（https://pep8-ja.readthedocs.io/ja/latest/）
    - 上から2番目の見出しに「一貫性にこだわりすぎるのは、狭い心の現れである」とある。似たような内容をSWEのDiscordで何度も見かけた。

- 書いたコードのスタイルをPEP8で見てみる
    - 変数の定義で、一行ずつ定義するか、一行で表現するか。GPTに調べさせたら特に記述なしだそう。
    - 空行について（https://pep8-ja.readthedocs.io/ja/latest/#id7）：特に問題なさそう。でも第二段階のウサギとカメのコードはちょっとあけすぎ？

- 背伸びしてドキュメントを覗いてみた
 - 組み込み（Built-in）の時間計算量、リストとハッシュテーブルを見てみた。https://docs.python.org/ja/3.16/builtins/time-complexity.html
 - Listオブジェクトのlist_containsを見てみた。短いけど意味わからない。https://github.com/python/cpython/blob/main/Objects/listobject.c#L656
 - Setオブジェクト＝ハッシュテーブルのset_do_lookupを見てみた。list_containsよりも親しみやすさを感じたけど、意味わからない。https://github.com/python/cpython/blob/main/Objects/setobject.c#L.221
## 第三段階
第二段階でやったことがそのまま連続で書けた

